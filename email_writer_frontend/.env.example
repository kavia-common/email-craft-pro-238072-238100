# Base URL of the FastAPI backend (no trailing slash), e.g. http://localhost:3001
NEXT_PUBLIC_API_BASE_URL=

# Optional: if set, overrides the post-login redirect destination
NEXT_PUBLIC_AFTER_LOGIN_PATH=/generator
